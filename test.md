Override the default branch name for new repositories → main

也就是把新 repository 的預設分支名稱設成 main。

現在 GitHub 新建立的 repository 通常都是以 main 作為預設分支，所以這樣之後你本機：

git init

建立的 repo 也會直接是 main，比較不容易遇到本地裝置叫 master、GitHub 叫 main，然後還要額外改名的情況。